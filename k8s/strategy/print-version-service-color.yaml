apiVersion: v1
kind: Service
metadata:
  namespace: strategy
  name: print-version
  labels:
    app: print-version
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: print-version # ①
    color: blue # ②
